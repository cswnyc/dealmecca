#!/usr/bin/env npx tsx

console.log('⚠️ Org chart migration disabled - schema not yet compatible');
console.log('📝 This script will be updated when org chart schema is implemented');
process.exit(0);